<div class="row">
<div class="col-md-10 offset-md-1 form-content">

<div class="outer-container">


<div id="search-content">
<h1>🔎 Search Concepts</h1>
  <div class="filter-container">
    <%= form_tag search_path, method: :get do %>
      <div class="filter-inner-container">
        <div class="filter-col">
          <%= label_tag 'Folder' %>
          <%= select_tag "folder", options_from_collection_for_select(@folders, 'id', 'name'), multiple: true, class: 'select2' %>
        </div>
        <div class="filter-col">
          <%= label_tag 'Document' %>
          <%= select_tag "document", options_from_collection_for_select(@documents, 'id', 'name'), multiple: true, class: 'select2' %>
        </div>
        <div class="filter-col">
          <%= label_tag 'Tag' %>
          <%= select_tag "tag", options_from_collection_for_select(@tags, 'id', 'name'), multiple: true, class: 'select2' %>
        </div>
        <%= submit_tag "Search", class: "btn btn-primary btn-orange filter-btn" %>
      </div>
    <% end %>
  </div>
<% unless @concepts.nil? %>
<% if @concepts.length > 0 %>
<br>
<%= link_to "Revise These Concepts", revision_sessions_concept_path(id: @concept_ids), method: :post, class: "btn btn-primary btn-orange btn-revise-search" %>
<br>
<% @concepts.each do |concepts| %>
    <div class="card-white">

      <b>🔎 <%= concepts.prompt %></b>
      <br>
      <%= concepts.content %>

    </div>
<% end %>
<% end %>
<% end %>
</div>
</div>
</div>
</div>
